# Keep text normalized in Git while preserving Windows batch files on checkout.
* text=auto eol=lf
*.bat text eol=crlf

source-android/app/src/main/assets/offline/ansagengenerator-offline-opus-data.zip.part-* -text
