# Keep text normalized in Git while preserving Windows batch files on checkout.
* text=auto eol=lf
*.bat text eol=crlf

# The production offline audio data pack is intentionally stored once via Git LFS.
source-android/app/src/main/assets/offline/ansagengenerator-offline-opus-data.zip filter=lfs diff=lfs merge=lfs -text
